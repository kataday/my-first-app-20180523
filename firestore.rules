service cloud.firestore {
  match /databases/{database}/documents {
    match /users {
      match /{uid} {
        allow read: if true;
        allow write: if request.auth.uid == uid;
      }
    }
  }
}
